# Image
FROM nvidia/cuda:12.2.0-base-ubuntu22.04

# Updates, additional packages, etc.
RUN  apt update && apt -q -y upgrade && apt -y install sudo && apt -y install vim

# Hence
CMD ["/bin/bash"]